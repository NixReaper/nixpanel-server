node_modules/
dist/
.env
*.env.local
*.log
.DS_Store
Thumbs.db

# package-lock.json is platform-specific (rollup native binaries differ between
# Windows dev machines and Linux servers). Never commit it — the server generates
# its own on each install.
package-lock.json

# Prisma
server/prisma/dev.db
server/prisma/dev.db-journal

# Build outputs
server/dist/
nixserver/dist/
nixclient/dist/

# Vite
nixserver/.vite/
nixclient/.vite/
